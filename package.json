{
    "devDependencies": {
        "bower": "^1.7.0",
        "grunt": "^0.4.5",
        "grunt-contrib-copy": "^0.8.0"
    },
    "scripts": {
        "postinstall": "bower install && grunt"
    }
}
